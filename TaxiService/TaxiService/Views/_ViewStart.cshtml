@inject SignInManager<IdentityUser> signInManager

@{
    if (signInManager.IsSignedIn(User) && User.IsInRole("Admin"))
    {
        Layout = "_LayoutAdmin";
    }
    else
    {
        Layout = "_Layout";
    }
}
